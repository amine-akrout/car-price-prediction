name: Deploy Web app to Google Cloud Run
on:
  push:
    branches:
      - main
jobs:
  deploy:
    name: Deploying
    runs-on: ubuntu-latest
    if: "contains(github.event.head_commit.message, 'deploy')"
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      # Build and push image to Google Container Registry
      - name: Setting up
        uses: google-github-actions/setup-gcloud@master
        with:
          version: '286.0.0'
          service_account_key: ${{ secrets.GCP_SA_KEY_JSON }}
          service_account_email: "akrout.med.amine@gmail.com"
          project_id: ml-dep-335415

      - name: Building (🏗)
        run: |-
          gcloud builds submit \
            --tag gcr.io/ml-dep-335415/carprice

      # Setup gcloud CLI
      - name: Deploy (🚀)
        uses: google-github-actions/deploy-cloudrun@main
        with:
          service: SERVICE_ID
          image: gcr.io/ml-dep-335415/carprice
          region: europe-west1
          credentials: ${{ secrets.GCP_SA_KEY_JSON }}
          project_id: ml-dep-335415
